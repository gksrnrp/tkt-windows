# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

tkt (terminal kakao talk) — 카카오톡 데스크톱 앱의 UI를 직접 읽고 조작하는 Ink 기반 TUI.
카카오톡 공식 API를 쓰지 않는다. macOS(Accessibility API)와 Windows(Win32 + 클립보드)
양쪽에서 동작하며, 플랫폼마다 네이티브 헬퍼가 따로 있다.

## 명령어

```bash
npm run dev          # build:native 후 tsx 로 src/cli.tsx 실행 (개발 기본 경로)
npm run build        # build:native 후 tsc → dist/
npm run start        # build:native 후 node dist/cli.js
npm run build:native # 플랫폼에 맞는 헬퍼 빌드 (scripts/build-native.mjs)
npm run doctor       # 시작이 막힐 때 단계별 진단 (scripts/doctor.mjs)
```

`build:native` 는 소스가 산출물보다 새것일 때만 다시 빌드한다. `npm run dev` 가 매번
이 스크립트를 거치므로, 안 그러면 실행할 때마다 컴파일을 기다려야 한다.

```bash
# macOS — swiftc 로 빌드
./native/tkt-ax check | windows | read <채팅방> [개수] | send <채팅방> <메시지> | open <채팅방>
```

```bash
# Windows — csc.exe(.NET Framework 4) 로 빌드
.\native\tkt-win.exe check | windows | send <채팅방> <메시지> | open <채팅방>
.\native\tkt-win.exe read <채팅방> [개수] [내이름]
.\native\tkt-win.exe raw <채팅방>     # 복사된 대화 원문 (진단용)
.\native\tkt-win.exe probe [채팅방]   # 창·UIA 트리 덤프 (진단용)
```

`src/*.ts(x)` 를 고쳤다면 `npm run dev` 만으로 반영된다. 네이티브 소스를 고쳤다면 재빌드가
필요한데, `npm run dev` 가 매번 빌드 스크립트를 타므로 그대로 쓰면 된다.

## 아키텍처

세 계층이 한 방향으로 쌓인다:

1. **네이티브 헬퍼** — UI를 직접 다루는 단발성 CLI. 명령마다 프로세스를 새로 띄우고
   JSON 한 줄을 stdout 으로 뱉은 뒤 `exit` 한다. 상태를 들고 있지 않다.
   - `native/tkt-ax.swift` (macOS, Accessibility API)
   - `native/tkt-win.cs` (Windows, Win32 + UI Automation + 클립보드)
2. **`src/kakao.ts`** — 헬퍼를 `execFile` 로 감싼 유일한 지점. 여기서 플랫폼 분기·직렬화·
   재시도·진단을 처리한다. 두 헬퍼는 **같은 CLI 계약**(명령 이름, JSON 모양, 오류 표식)을
   지키므로 위 계층은 플랫폼을 거의 신경 쓰지 않는다.
3. **`src/app.tsx`** — Ink UI. 폴링 루프 하나가 `readMessages` 를 반복 호출한다.

`src/cli.tsx` 는 진입점으로, 준비 상태 확인(`missingTools`) → 설정 로드/`setup` →
`render(<App/>)` 순이다.

### 채팅방은 "창 제목"으로 식별된다

chat_id 같은 안정적인 키가 없다. `config.json` 의 `displayName` 이 곧 카카오톡 창 제목이며,
읽기·전송·열기 모두 이 문자열로 창을 찾는다. 두 헬퍼 모두 **정확 일치 우선, 부분 일치는
후보가 하나일 때만** 채택한다. "김학재" 는 "김학재 가족방" 에도 걸리므로, 모호하면 아무것도
하지 않는다 — 읽기는 화면만 이상해지지만 전송은 되돌릴 수 없다.

### 헬퍼 호출은 반드시 직렬

`kakao.ts` 의 `serialize()` 큐가 read/send/open 을 한 줄로 세운다. 두 호출이 겹치면 같은
UI를 동시에 건드려 서로를 망친다. 예외는 `listOpenChats()` — 창 제목만 훑는 가벼운 조회라
큐를 타지 않는다. 큐에 넣으면 읽기가 도는 동안 Tab(채팅방 전환)이 먹지 않는다.

### 오류 표식 `열려 있지 않습니다`

`kakao.ts` 의 `WINDOW_CLOSED` 는 "채팅창이 닫혀 있다"는 신호이고, 이걸 보면 `open` 을 태운
뒤 다시 읽는다. **두 헬퍼 모두** 다른 오류 문구에 이 말이 섞이면 안 된다 (특히 "이름이
모호하다" 오류). 섞이면 닫힌 창으로 오해해 아무 방이나 열게 된다.

---

## macOS (`native/tkt-ax.swift`)

### 전송 경로는 두 가지, 기본은 키 이벤트 없는 경로

`sendWithoutKeyboard` 가 기본이다. `AXSelectedTextRange` 로 기존 입력 내용을 통째로 선택한 뒤
`AXSelectedText` 로 치환하고 「전송」 버튼을 `AXPress` 한다. 이 경로는 카카오톡 창을 앞으로
끌어올리지 않는다 — 이 도구의 존재 이유다.

- `AXValue` 를 직접 세팅하면 안 된다. 카카오톡 내부 텍스트 모델이 그 값을 모르고 전송이 무시된다.
- 「전송」 버튼은 **본문을 넣은 뒤에** 찾아야 한다. 입력창이 비어 있으면 버튼이 그려지지 않을 수 있다.
- 버튼은 아이콘이라 `AXTitle` 이 비어 있을 수 있으니 `AXDescription` 까지 본다 (`buttonLabels`).
- `AXPress` 의 반환값은 신뢰할 수 없다. **입력창이 비워졌는지**로 성공을 판정한다.
- 실패 시에만 키보드 fallback(`CGEvent.postToPid`)으로 떨어지며, 이 경로는 창을 앞으로 올린다.

### 읽기의 휴리스틱

`runRead` 는 대화 영역(AXTable을 가진 AXScrollArea)의 행을 훑는다:

- **내가 보낸 메시지 판정**: 말풍선 프레임의 `maxX` 가 테이블 오른쪽 끝에서 40pt 이내면 내 것.
- **시각 상속**: 카카오톡은 같은 분에 연속된 메시지의 마지막 행에만 시각을 표시하므로,
  뒤에서 앞으로 훑으며 빈 시각을 채운다.
- 본문 없이 AXImage 만 있는 행은 `[사진]` / `[사진 N장]`, 그 외(날짜 구분선 등)는 버린다.

### AX 트리 순회 시 대화 영역은 반드시 건너뛴다

`findInputField` / `findSendButton` 은 `isTranscriptArea()` 로 대화 영역을 잘라낸다. 말풍선도
`AXTextArea` 라서 수백 개를 훑게 되고, 그러면 전송 한 번에 20초가 걸린다. 새 탐색 함수를 추가할
때도 같은 가드를 넣을 것.

### 창 상태 판별의 함정

최소화하거나 앱을 숨기면(Cmd+H) 창의 subrole 이 `AXStandardWindow` → `AXDialog` 로 바뀌지만 AX
트리는 살아 있어 읽기·전송이 모두 된다. `chatWindows()` 가 최소화·숨김 상태의 `AXDialog` 를
채팅창으로 인정하는 이유다. 이걸 걸러내면 최소화한 창을 "닫힌 방"으로 오해해 `open` 이 돌고,
사용자가 내려둔 창이 도로 튀어나온다.

---

## Windows (`native/tkt-win.cs`)

### 창 구조 (카카오톡 26.x 기준, 실측)

| 대상 | 클래스 | 비고 |
|---|---|---|
| 메인 창(채팅 목록) | `EVA_Window_Dblclk` | 제목 `카카오톡` |
| 채팅방 창 | `EVA_Window_Dblclk` | 제목이 채팅방 이름. 메인 창과 클래스가 같다 |
| 입력창 | `RICHEDIT50W` | **대문자**. 클래스 비교는 대소문자를 무시할 것 |
| 대화 목록·채팅 목록 | `EVA_VH_ListControl_Dblclk` | owner-drawn |

채팅방 창은 클래스만으로 메인 창과 구분되지 않는다. **입력창(`RICHEDIT50W`)을 품고 있는지**
까지 봐야 확정할 수 있다. 예전 버전은 채팅방을 `#32770` 으로 띄웠으므로 두 클래스 다 받는다.

### 읽기 — 접근성 API가 통째로 막혀 있다

이것이 macOS 판과 근본적으로 다른 지점이다. 대화 목록은 owner-drawn 이면서 접근성에
아무것도 내놓지 않는다. 실측 결과:

- `WM_GETTEXT` → 빈 문자열
- UI Automation → 자식이 스크롤바뿐, **지원하는 패턴이 0개** (TextPattern 조차 없다)
- 레거시 MSAA(`IAccessible`) → 시스템 메뉴와 스크롤바만

그래서 **앱이 직접 만들어주는 복사 결과**를 쓴다. 대화 목록에 `Ctrl+A` → `Ctrl+C` 를 보내고
클립보드에서 받아온다. 형식은 `[이름] [오후 3:22] 본문`.

- **`mine` 판정에 내 이름이 필요하다.** 복사본에는 내 메시지도 내 이름으로 적혀서, 위치로
  판단하는 macOS 와 달리 이름을 비교할 수밖에 없다. `config.json` 의 `myName` 이 그것이고
  `read` 의 네 번째 인자로 전달된다.
- 날짜 구분선(`2026년 7월 9일 목요일`)은 대괄호로 시작하지 않아서 여러 줄 메시지의 둘째
  줄과 겉모습이 같다. `IsDateSeparator` 가 요일 이름 + 숫자 5개 이상으로 가려낸다.
- 시각은 `오후 3:22` → `15:22` 로 정규화한다. 화면의 시각 칸이 여섯 글자 폭이라 그대로는
  밀리고, macOS 헬퍼의 출력 모양과도 맞춘다.

### SetFocus 를 부르면 안 된다

`AttachThreadInput` 으로 입력 큐를 붙인 상태에서 `SetFocus` 를 하면 **그 창이 활성화되면서
화면 맨 앞으로 올라온다.** 포커스를 강제로 뺏을 때 쓰는 기법이 정확히 이 조합이다. 처음에는
"컨트롤에 포커스가 있어야 키가 먹겠지" 싶어 넣었는데, 실측해 보니 `SetFocus` 없이도 복사는
똑같이 성공했고 넣은 쪽만 읽기마다 카카오톡 창을 사용자가 보던 창 위로 끌어올렸다.

키 메시지는 컨트롤 핸들로 직접 보내므로 포커스가 필요 없다. `AttachThreadInput` 은
`SetKeyboardState` 로 만든 Ctrl 눌림 상태를 카카오톡과 공유하는 용도로만 쓴다.

### 카카오톡 잠금모드

카카오톡은 사용자가 일정 시간 앱을 만지지 않으면 잠근다. tkt 가 보내는 것은 합성 메시지라
"사용 중" 으로 쳐주지 않으므로, tkt 로 대화하는 동안에도 잠금이 걸린다. 잠기면 **열어둔
채팅방 창이 모두 닫히고** 메인 창이 잠금 화면으로 덮인다.

그러면 "채팅창이 열려 있지 않습니다" 만 보이는데, 창을 닫은 적 없는 사용자는 영문을 모른다.
`IsLocked()` 가 메인 창의 `LockModeView` 자식이 보이는지로 잠금을 판별해서, `check` 의
`locked` 필드와 `NotOpenMessage` 가 원인을 짚어준다. 근본 해결은 사용자가 카카오톡 설정에서
자동 잠금을 끄는 것뿐이다 — 우리가 가짜 입력으로 잠금을 막는 짓은 하지 않는다.

### 대화 목록에 Esc 를 보내면 안 된다

카카오톡에서 `Esc` 는 **채팅방 창을 닫는 키**다. `Ctrl+A` 로 물든 선택을 지우려고 Esc 를
보냈다가, 읽기 한 번에 창이 사라지고 그 뒤로는 "창이 없다 → `open` 시도 → 실패" 가 폴링마다
반복되는 일이 있었다. 선택 표시가 남는 편이 훨씬 낫다 — 지금은 선택을 그냥 둔다.

같은 이유로 `open` 은 메인 창을 억지로 띄우지 않는다. 채팅방 창이 닫힌 채로 두면 폴링이
몇 초마다 `open` 을 다시 부르는데, 거기서 `ShowWindow`/`SetForegroundWindow` 를 하면 카카오톡
메인 창이 계속 화면 앞으로 튀어나온다. 메인 창이 안 보이면 이유만 알리고 물러난다.

### 키를 먹이려면 SendMessage 와 PostMessage 를 둘 다 보내야 한다

`SendKeyEvent` 가 그렇게 한다. **`SendMessage` 만으로는 커스텀 리스트 컨트롤이 반응하지
않는다** — 자체 메시지 루프에서 스레드 큐를 직접 훑는 구조라 큐에 실제로 들어가는
`PostMessage` 가 있어야 키를 본다. 반대로 `PostMessage` 만 남기면 처리 시점이 어긋나 복사가
빈 채로 끝나는 일이 있었다. 키가 두 번 처리될 수 있지만 `Ctrl+A`·`Ctrl+C`·`Esc` 는 모두
여러 번 눌러도 결과가 같다.

`lParam` 도 실제 키 입력과 같은 모양(반복 횟수 1 + 스캔코드, 뗄 때는 전이 비트)으로 채워야
한다. 0 으로 두면 무시된다.

### 포커스를 뺏지 않는 방법

`AttachThreadInput` 으로 우리 스레드의 입력 큐를 카카오톡 스레드에 붙이면 `SetFocus` 가
통하고, `SetKeyboardState` 로 만든 Ctrl 눌림 상태를 카카오톡도 같이 본다. `SetForegroundWindow`
는 쓰지 않는다 — 쓰면 사용자가 보던 창 위로 카카오톡이 튀어나와 이 도구의 존재 이유가 사라진다.

**Ctrl 눌림 상태는 무슨 일이 있어도 되돌려야 한다.** 남겨두면 사용자의 다음 키 입력이 전부
단축키가 된다. `SendKeysTo` 의 `finally` 가 그 일을 한다.

### 클립보드는 반드시 원래대로 되돌린다

읽기 한 번마다 사용자의 클립보드를 빌린다. `Clipboard.ReadText()` 는 실패(null)와 빈
클립보드("")를 구분하는데, 이걸 뭉뚱그리면 복원 단계에서 원래 내용을 지워버린다. 되돌릴 수
있는 것은 텍스트뿐이라 이미지·파일은 잃는다 — README 에 적어둔 제약이다.

이 왕복이 1초 가까이 걸려서 `DEFAULTS.pollIntervalMs` 가 Windows 에서는 3초다.

### `open` 은 사실상 지원되지 않는다

채팅 목록도 같은 커스텀 컨트롤이라 방 행을 짚을 수 없다. 검색칸(`Edit`)에 이름을 넣고
Enter 를 보내는 경로가 남아 있지만, 카카오톡은 검색칸을 늘 그려두지 않아서(사용자가 검색
아이콘을 누르기 전에는 폭 0) 대개 실패한다. 실패해도 무엇이 왜 안 되는지 그대로 알린다.
쓰려는 방은 사용자가 카카오톡에서 직접 열어두는 것이 정상 경로다.

### 진단 명령

카카오톡 버전이 올라가 깨졌을 때 원인을 가르는 통로다. `raw` 는 복사 형식이 바뀌었는지,
`probe` 는 창 클래스와 UIA 노출이 바뀌었는지 보여준다. 둘 다 Windows 전용이다.

---

## 화면 높이는 터미널을 절대 넘으면 안 된다

넘는 순간 Ink 가 프레임을 다시 그릴 때마다 터미널이 스크롤해서, **글자를 한 자 칠 때마다
화면이 위아래로 튄다.** 타이핑이 불가능해진다. 그래서 `app.tsx` 의 레이아웃에는 규칙이 있다:

- 대화 상자는 `minHeight` 가 아니라 **`height` 로 고정**하고 `overflow="hidden"` 을 준다.
  `minHeight` 는 최소값이라 내용이 늘어나면 위로 자라는 것을 막지 못한다.
- 보여줄 메시지는 **개수가 아니라 화면 줄 수로 잘라야 한다.** 긴 메시지는 여러 줄로 접히므로
  `slice(-n)` 으로 자른 목록은 상자보다 훨씬 길어진다. `lineCount()` 로 접힌 줄까지 세서
  뒤에서부터 채운다.
- 폭을 잴 때는 글자 수가 아니라 **칸 수**를 쓴다 (`displayWidth`). 한글은 두 칸이라 글자 수로
  세면 들어갈 줄 수를 두 배로 착각한다. 이름·시각 칸 정렬도 `padDisplay` 로 맞춘다.
- 아래쪽은 평소 입력 한 줄이지만 `Tab` 을 누르면 채팅방 목록 상자로 바뀐다. 그만큼 대화
  상자를 줄여야 한다 — 안 그러면 목록을 열 때마다 화면이 통째로 밀린다. 열린 방이 많을
  때를 대비해 목록도 고른 항목 둘레만 보여준다.
- 마지막으로 한 줄은 여유로 남긴다. 화면을 정확히 꽉 채우면 터미널에 따라 한 줄이 밀린다.

## 한글 조합 글자와 터미널 커서 (`src/ime-cursor.ts`)

Windows 의 IME 는 조합 중인 글자를 **터미널 커서가 있는 칸** 에 그린다. 그런데 Ink 는 프레임
끝에 항상 개행을 붙여서, 다 그리고 나면 커서가 프레임 맨 아래 **다음 줄** 에 있다. 그대로 두면
조합 중인 한글이 입력줄이 아니라 아래 줄에 나타났다가 완성되는 순간 입력줄로 뛰어오른다.
영문은 조합 과정이 없어서 멀쩡하고 한글에서만 티가 난다.

두 가지로 막는다:

1. **입력줄을 화면의 마지막 줄로 둔다.** 안내·오류를 입력줄 위로 올렸다. 이것만으로 조합
   글자가 두 줄 아래에서 한 줄 아래로 붙는다.
2. **커서를 직접 옮긴다.** `withImeCursor()` 가 Ink 의 stdout 을 감싸서, 프레임을 쓸 때마다
   뒤에 "한 줄 위 N칸으로" 를 덧붙인다. N 은 `app.tsx` 가 렌더할 때마다 `imeCursor.column`
   에 넣어주는 값이다 (왼쪽 여백 1 + `"> "` 2 + 입력한 글자 폭, 그다음 칸).

**되돌리는 것이 이 장치의 핵심이다.** Ink 는 이전 화면을 지울 때 커서가 프레임 바로 아래에
있다고 전제하고 "위로 N줄 지우기" 를 보낸다. 옮겨둔 채로 다음 프레임을 쓰면 지우는 줄이 한
줄씩 밀려 화면이 부서진다. 그래서 래퍼는 **다음 출력 맨 앞에 반드시 복귀 제어문자를 붙이고**,
앱을 끝낼 때도 `restore()` 로 제자리에 돌려놓는다 (안 그러면 셸 프롬프트가 입력줄에 겹친다).

`imeCursor.column` 은 React 상태가 아니라 그냥 값이고 **렌더 본문에서** 갱신한다. 이 값을 읽는
쪽이 React 바깥(Ink 가 화면에 쓰는 순간)이고, Ink 는 커밋 단계에서 쓰는데 `useEffect` 는 그보다
늦게 돌아 한 프레임 뒤처진 자리를 가리키기 때문이다. 채팅방 목록이 떠 있거나 전송 중이면
입력줄이 없으므로 `null` 을 넣어 옮기지 않는다.

## UI 폴링 루프의 제약

`app.tsx` 의 폴링 이펙트에서 `active` 플래그는 **반드시 이펙트 안의 지역 변수**여야 한다. ref 로
공유하면 방 전환 시 이전 루프가 죽지 않고 두 루프가 번갈아 `setRows` 를 호출해 화면이 두 채팅방을
오간다. `wake` ref 는 전송 직후 폴링을 즉시 깨우는 신호다.

## 로케일 의존

카카오톡 UI 언어에 묶인 문자열이 각 헬퍼 상단에 모여 있다.

- macOS: `chatListWindowTitles`(채팅 목록 창 제목), `sendButtonTitles`(전송 버튼),
  `pressWindowMenuItem` 에 넘기는 `["채팅", "Chats"]`
- Windows: `MainWindowTitles`(메인 창 제목), `IsDateSeparator` 의 요일 이름 목록

한국어·영어만 지원하며 다른 언어 대응은 여기를 고친다.

## 설정

`config.json` 은 프로젝트 루트에 있고 **gitignore 대상**이다 (대상 채팅방 이름과 사용자
이름이 들어간다). `src/config.ts`, `src/kakao.ts` 모두 `import.meta.url` 기준 `../` 로 경로를
잡는데, `src/` 와 `dist/` 가 똑같이 루트 한 단계 아래라 개발·빌드 양쪽에서 같은 곳을 가리킨다.
이 구조를 깨는 디렉터리 이동은 두 파일의 경로 계산을 함께 고쳐야 한다.

## 사용자 대상 문구

에러 메시지, 주석, README 모두 한국어다. 특히 `kakao.ts` 의 `missingTools()` 는 준비되지 않은
항목의 안내를 여러 줄로 돌려주며 `cli.tsx` 가 들여쓰기해 출력한다. 새 안내를 추가할 때 이
형식을 유지할 것. 안내 문구는 플랫폼별로 갈린다 (macOS 는 손쉬운 사용 권한, Windows 는 권한
수준·로그인 여부).
